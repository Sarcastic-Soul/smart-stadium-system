# Cloud profile - enables Firestore and Pub/Sub
spring.profiles.active=cloud

# Remove Firestore auto-config exclusion (it's needed in cloud)
spring.autoconfigure.exclude=

# Enable Pub/Sub
spring.cloud.gcp.pubsub.enabled=true
